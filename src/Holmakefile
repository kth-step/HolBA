INCLUDES = core 
OPTIONS = QUIT_ON_FAILURE

ifdef POLY
HOLHEAP = HolBA-heap
EXTRA_CLEANS = $(HOLHEAP) $(HOLHEAP).o

BARE_THYS = core/HolBACoreTheory
DEPS = $(patsubst %,%.uo,$(BARE_THYS))

$(HOLHEAP): $(DEPS)
	$(protect $(HOLDIR)/bin/buildheap) -o $(HOLHEAP) $(BARE_THYS)

endif
